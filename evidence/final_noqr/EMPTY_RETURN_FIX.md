# 유효 반환점0 패킷 처리

v6의5초pilot01은 상태/RGB0개에서 중단됐다. `raw/rover_000004.npy`는 기존 엄격한 구조검사에 통과한4800항목의 패킷이며 flags는 전부0이다. 원본SHA390de58025b3046409c2e7de888b4e928b0675d7950a67f7e1550257b444e7fb. 시간범위 min/max를 빈 배열에 적용한 것이 직접 오류 원인이다. native 출력이 그 순간0반환이 된 엔진 내부 원인은 확인하지 않았다.

v7은 이러한 정상구조 패킷을 raw와 width=0 PointCloud2로 보존하고, 유효점시각 min/max를null로 표시한다. 그 native frame 구간도 전체구간 후보검색에 포함한다. 2회완료 후 마지막 정상nonempty 선택은 유지하며0점패킷을 선택/내용검증 성공으로 취급하지 않는다. 정상nonempty 패킷의 시각/점/직렬화는 그대로다. 1초·5초 회귀와한정replay를 새v7에서 재수행한 뒤 최종30초 gate를 판단한다.
